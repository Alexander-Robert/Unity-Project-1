using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UpdateAllCubes : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //EX of affected grouped objects using foreach and calling their specific script functions
    // public static void ButtonWasClicked() {
    //     foreach (Transform child in this.GetComponent<Transform>()) {
    //         //calls the function from the ReactToButton script component of the child object!
    //         child.GetComponent<ReactToButton>().ButtonWasClicked();
    //     }
    // }
}
